name: CI

on:
  pull_request:
    branches: [ main, feat/** ]
  push:
    branches: [ main ]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  RAILS_ENV: test
  DATABASE_URL: postgres://postgres:postgres@localhost:5432/coinbase_futures_bot_test
  COVERAGE: true

jobs:
  changes:
    name: Detect Changed Files
    runs-on: ubuntu-latest
    outputs:
      docs_only: ${{ steps.classify.outputs.docs_only }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Classify changes
        id: classify
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
        run: |
          set -euo pipefail

          if [ "$GITHUB_EVENT_NAME" != "pull_request" ]; then
            echo "docs_only=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          BASE_REF="${GITHUB_BASE_REF:-main}"
          git fetch origin "$BASE_REF" --depth=1

          CHANGED_FILES="$(git diff --name-only "origin/$BASE_REF...HEAD")"

          if [ -z "$CHANGED_FILES" ]; then
            echo "docs_only=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          DOCS_ONLY=true
          while IFS= read -r file; do
            case "$file" in
              README.md|SESSION_NOTES.md|AGENTS.md|CLAUDE.md|CONTEXT.md|docs/*|wiki/*|.github/**/*.md)
                ;;
              *)
                DOCS_ONLY=false
                break
                ;;
            esac
          done <<EOF
          $CHANGED_FILES
          EOF

          echo "docs_only=$DOCS_ONLY" >> "$GITHUB_OUTPUT"

  lint:
    name: StandardRB (lint)
    runs-on: ubuntu-latest
    needs: [changes]
    steps:
      - uses: actions/checkout@v7

      - uses: ruby/setup-ruby@v1
        if: needs.changes.outputs.docs_only != 'true'
        with:
          ruby-version-file: '.ruby-version'
          bundler-cache: true

      - name: Lint code
        if: needs.changes.outputs.docs_only != 'true'
        run: bin/standardrb

      - name: Skip lint for docs-only PR
        if: needs.changes.outputs.docs_only == 'true'
        run: echo "Docs-only PR -> skipping StandardRB"

  security:
    name: Security scans
    runs-on: ubuntu-latest
    needs: [changes]
    steps:
      - uses: actions/checkout@v7

      - uses: ruby/setup-ruby@v1
        if: needs.changes.outputs.docs_only != 'true'
        with:
          ruby-version-file: '.ruby-version'
          bundler-cache: true

      - name: Brakeman (static analysis)
        if: needs.changes.outputs.docs_only != 'true'
        run: bin/brakeman --no-pager

      - name: Bundler Audit (dependency vulnerabilities)
        if: needs.changes.outputs.docs_only != 'true'
        # Temporary ignore for pre-existing nokogiri advisories in this branch.
        # TODO(#187): remove these ignores once nokogiri is upgraded.
        # Keep auditing all other gems in the meantime.
        run: bundle exec bundler-audit check --update --ignore GHSA-c4rq-3m3g-8wgx --ignore GHSA-v2fc-qm4h-8hqv

      - name: Skip security scans for docs-only PR
        if: needs.changes.outputs.docs_only == 'true'
        run: echo "Docs-only PR -> skipping security scans"

  test:
    name: Rails tests with coverage
    runs-on: ubuntu-latest
    needs: [changes, lint, security]

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v7
        if: needs.changes.outputs.docs_only != 'true'
        with:
          fetch-depth: 0  # Fetch full history for diff comparison

      - uses: ruby/setup-ruby@v1
        if: needs.changes.outputs.docs_only != 'true'
        with:
          ruby-version-file: '.ruby-version'
          bundler-cache: true

      - name: Install system dependencies
        if: needs.changes.outputs.docs_only != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y libpq-dev pkg-config postgresql-client

      # Cache only the runtime-balance map used by parallel_rspec. VCR cassettes
      # are committed to git, so caching them is redundant; spec/**/* in the key
      # busted the cache on nearly every PR. A stable key keyed on Gemfile.lock
      # keeps a recent balance map warm without needing an exact match.
      - name: Cache parallel test runtime map
        if: needs.changes.outputs.docs_only != 'true'
        uses: actions/cache@v6
        with:
          path: tmp/parallel_runtime_rspec.log
          key: ${{ runner.os }}-rspec-runtime-${{ hashFiles('Gemfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-rspec-runtime-

      - name: Prepare database (create + migrate)
        if: needs.changes.outputs.docs_only != 'true'
        run: |
          echo "Waiting for PostgreSQL service to be ready..."
          # Wait for PostgreSQL to be fully ready
          until pg_isready -h localhost -p 5432 -U postgres; do
            echo "PostgreSQL not ready yet, waiting..."
            sleep 2
          done
          echo "PostgreSQL is ready!"

          # Create both numbered worker DBs (--first-is-1 means workers use suffix 1 and 2)
          TEST_ENV_NUMBER=1 bin/rails db:create db:migrate
          TEST_ENV_NUMBER=2 bin/rails db:create db:migrate
          echo "Database setup complete"

      - name: Run tests with coverage
        if: needs.changes.outputs.docs_only != 'true'
        timeout-minutes: 15
        env:
          PARALLEL_RSPEC_CONFIG: .parallel_rspec_config.ci
          PARALLEL_COVERAGE: "true"
          SAMPLE: ""
          RPROF: ""
          STACKPROF: ""
          TAG_PROF: ""
          FORCE_REAL_TESTS: "true"
          VCR_RECORD_MODE: none
          CI: true
          COVERAGE: true
        run: |
          # Control exit code manually so coverage checks still see a result.
          set +e

          bin/parallel_rspec
          TEST_EXIT_CODE=$?

          if [ -f "coverage/.resultset.json" ]; then
            echo "✅ Coverage file generated: coverage/.resultset.json"
          else
            echo "❌ Coverage file not found"
          fi

          if [ $TEST_EXIT_CODE -eq 0 ]; then
            echo "✅ Tests passed successfully"
          else
            echo "❌ Tests failed with exit code $TEST_EXIT_CODE"
          fi

          exit $TEST_EXIT_CODE

      - name: Upload coverage reports
        if: always() && needs.changes.outputs.docs_only != 'true'
        uses: actions/upload-artifact@v7
        with:
          name: coverage-reports-${{ github.run_number }}
          path: |
            coverage/
            coverage/.resultset.json
            coverage/index.html
            tmp/rspec.xml
            tmp/spec_summary.log
            tmp/failing_specs.log
          retention-days: 30

      - name: Check coverage thresholds
        if: success() && needs.changes.outputs.docs_only != 'true'
        run: |
          echo "=== COVERAGE THRESHOLD CHECK ==="

          if [ -f "coverage/.resultset.json" ]; then
            echo "Coverage data found, checking thresholds..."

            # Extract line coverage percentage from JSON
            LINE_COVERAGE=$(ruby -rjson -e "
              data = JSON.parse(File.read('coverage/.resultset.json'))
              if data['RSpec'] && data['RSpec']['coverage']
                line_coverage = data['RSpec']['coverage'].values.map { |v| v['lines'] }.flatten.compact
                covered = line_coverage.count { |v| v && v > 0 }
                total = line_coverage.count { |v| v }
                percentage = total > 0 ? (covered.to_f / total * 100).round(2) : 0
                puts percentage
              else
                puts '0'
              end
            ")

            # Extract branch coverage percentage from JSON (handle different structure)
            BRANCH_COVERAGE=$(ruby -rjson -e "
              data = JSON.parse(File.read('coverage/.resultset.json'))
              if data['RSpec'] && data['RSpec']['coverage']
                # Branch coverage structure is different - it's nested under each file
                total_branches = 0
                covered_branches = 0

                data['RSpec']['coverage'].each do |file_path, coverage_data|
                  if coverage_data['branches']
                    coverage_data['branches'].each do |branch_id, branch_data|
                      total_branches += 1
                      if branch_data && branch_data.is_a?(Array) && branch_data.any? { |v| v && v > 0 }
                        covered_branches += 1
                      end
                    end
                  end
                end

                percentage = total_branches > 0 ? (covered_branches.to_f / total_branches * 100).round(2) : 0
                puts percentage
              else
                puts '0'
              end
            ")

            echo "Line coverage: ${LINE_COVERAGE}% (threshold: 50%)"
            echo "Branch coverage: ${BRANCH_COVERAGE}% (threshold: 25%)"

            # Check if thresholds are met (using realistic thresholds for current coverage)
            if (( $(echo "$LINE_COVERAGE >= 50" | bc -l) )); then
              echo "✅ Line coverage threshold met"
            else
              echo "⚠️  Line coverage below threshold (50%) - Consider improving test coverage"
              # Don't fail the build, just warn
            fi

            if (( $(echo "$BRANCH_COVERAGE >= 25" | bc -l) )); then
              echo "✅ Branch coverage threshold met"
            else
              echo "⚠️  Branch coverage below threshold (25%) - Consider improving test coverage"
              # Don't fail the build, just warn
            fi

            echo "📊 Coverage report generated successfully!"
            echo "💡 To improve coverage:"
            echo "   - Add tests for untested methods and branches"
            echo "   - Use SimpleCov locally: rake coverage:run"
            echo "   - Review coverage report: coverage/index.html"
          else
            echo "❌ Coverage data not found"
            exit 1
          fi

      - name: Skip tests for docs-only PR
        if: needs.changes.outputs.docs_only == 'true'
        run: echo "Docs-only PR -> skipping Rails test suite"

  # Coverage badge generation - runs at the end of successful CI
  coverage-badge:
    name: Generate Coverage Badge
    runs-on: ubuntu-latest
    needs: [changes, lint, security, test]
    if: always() && needs.lint.result == 'success' && needs.security.result == 'success' && needs.test.result == 'success'
    steps:
      - name: Skip coverage badge for docs-only PR
        if: needs.changes.outputs.docs_only == 'true'
        run: echo "Docs-only PR -> skipping coverage badge generation"

      - name: Download coverage artifacts
        if: needs.changes.outputs.docs_only != 'true'
        uses: actions/download-artifact@v8
        with:
          name: coverage-reports-${{ github.run_number }}
          path: coverage/
        continue-on-error: true

      - name: Generate coverage badge
        if: needs.changes.outputs.docs_only != 'true'
        run: |
          echo "=== COVERAGE BADGE GENERATION ==="
          echo "Looking for coverage data under coverage/"

          COVERAGE_JSON=$(find coverage -type f -name coverage.json | head -n 1)

          if [ -n "$COVERAGE_JSON" ]; then
            echo "✅ Coverage data found at: $COVERAGE_JSON"
            # Extract line coverage percentage from SimpleCov JSON format
            LINE_COVERAGE=$(ruby -rjson -e "
              data = JSON.parse(File.read(ARGV[0]))
              if data['metrics'] && data['metrics']['covered_percent']
                puts data['metrics']['covered_percent'].round(0)
              else
                puts '0'
              end
            " "$COVERAGE_JSON")

            echo "Line coverage: ${LINE_COVERAGE}%"

            # Determine badge color based on coverage
            if [ "$LINE_COVERAGE" -ge 90 ]; then
              COLOR="brightgreen"
            elif [ "$LINE_COVERAGE" -ge 80 ]; then
              COLOR="green"
            elif [ "$LINE_COVERAGE" -ge 70 ]; then
              COLOR="yellowgreen"
            elif [ "$LINE_COVERAGE" -ge 60 ]; then
              COLOR="yellow"
            else
              COLOR="red"
            fi

            # Create badge URL
            BADGE_URL="https://img.shields.io/badge/coverage-${LINE_COVERAGE}%25-${COLOR}?style=flat-square"

            echo "📊 Coverage Badge URL: $BADGE_URL"
            echo "💡 To update your README.md with this badge, run:"
            echo "   echo '![coverage]($BADGE_URL)' >> README.md"
          else
            echo "❌ Coverage data not found"
            echo "Available files in coverage directory:"
            find coverage -maxdepth 3 -type f | sort || echo "Coverage directory does not exist"
            echo "This usually means the CI workflow didn't generate coverage data or the artifact name mismatch"
            exit 1
          fi

      - name: All checks passed
        run: echo "🎉 All CI checks passed successfully!"
