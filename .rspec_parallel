# `-O .rspec_parallel` replaces default `.rspec`; keep spec_helper so SimpleCov + RSpec config match.
--require spec_helper
--color
--format progress
--format ParallelTests::RSpec::RuntimeLogger
--out tmp/parallel_runtime_rspec.log
--order random
