# Session Notes

### Session log

- 2026-07-17: Shipped the operator-visibility epic (#239) — sentiment pipeline + TUI/CLI surfacing. `Sentiment::Snapshot` (read-only) feeds `bin/futuresbot status`, the TUI system strip, and the Health tab (key `5`). `Sentiment::PipelineRunner` runs fetch→score→aggregate inside `bin/futuresbot start` (opt out with `FUTURESBOT_SKIP_SENTIMENT_PIPELINE`, cadence via `SENTIMENT_PIPELINE_INTERVAL_SECONDS`, default 120); deployed Rails still uses GoodJob cron for the same jobs. NOL/OIL news maps to the `OIL-USD` sentiment symbol via `Sentiment::ContractSymbolMapper`, and aggregation follows enabled contracts. **Verify sentiment is collecting:** run `bin/futuresbot status` and confirm the Sentiment section shows a per-symbol z-score (e.g. `OIL-USD: z=-0.4 (3/15m)`) with a recent `Last event` age; `No sentiment data yet` or `⚠ STALE` means the pipeline is idle. Enable futures products first (`bin/rake market_data:upsert_futures_products`) so enabled-contract → sentiment-symbol resolution works. The TUI dashboard is now tab-based (`1`-`5`) with a persistent system strip; the Market tab splits futures/spot into framed subpanels, and Positions/Signals tabs have actionable empty states. Stale threshold: `SENTIMENT_STALE_THRESHOLD_MINUTES` (default 30).
- 2026-06-16: Bumped indirect `net-imap` in `Gemfile.lock` from `0.6.4` to `0.6.4.1` to clear `bundler-audit` CVEs blocking unrelated dependency PR security checks. Verified `bundle exec bundler-audit check --update --ignore GHSA-c4rq-3m3g-8wgx --ignore GHSA-v2fc-qm4h-8hqv` reports no vulnerabilities.
